#include <iostream>
#include <conio.h>
#include <vector>
using namespace std;
int cost(int index, int arr[])
{
    int one;
    int two;
    if (index == 0)
    {
        return 0;
    }
    one = cost

}
int main()
{
    int arr[] = {10, 20, 30, 10};
    cost(0, arr);
    getch();
    return 0;
}
